require 'rails_helper'
describe ConnpassScraping do
  # let(:event) { Api::Connpass.new.search(event_id: 51037).first }
  # let(:scraping) { Scraping::Connpass.new(event) }
  # it '#logo' do
  #   expect(scraping.logo).to eq 'aaaa'
  # end
  # let(:api) { Connpass.new }
  # it '１ページ(１００件以内)の場合' do
  #   result = api.search(['名古屋'], ['201701'])
  #   expect(result.count).to be < 100
  # end
  # it '２ページ(１００件以上)の場合' do
  #   result = api.search(['名古屋'], ['201610', '201611'])
  #   expect(result.count).to be >= 100
  # end
  # it '３ページ(２００件以上)の場合' do
  #   result = api.search(['愛知', '名古屋'], ['201610', '201611', '201612', '201701'])
  #   expect(result.count).to be >= 200
  # end
end
